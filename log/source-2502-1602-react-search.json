{
  "content": "<p>Init the value with search params like this:</p>\n<pre><code>const urlParams = new URLSearchParams(window.location.search)\nconst defaultQuery = urlParams.get('search') || ''\nconst [query, setQuery] = useState(defaultQuery)\n</code></pre>\n<p>Update the search params with query value like this:</p>\n<pre><code>const urlParams = new URLSearchParams()\nurlParams.set('search', query)\nwindow.history.replaceState({}, '', window.location.pathname + '?' + urlParams.toString())\n</code></pre>",
  "id": "source-2502-1602-react-search",
  "tags": [
    "react",
    "web"
  ],
  "title": "How to sync state value with search params",
  "text": "Init the value with search params like this: const urlParams = new URLSearchParams(window.location.search) const defaultQuery = urlParams.get('search') || '' const [query, setQuery] = useState(defaultQuery) Update the search params with query value like this: const urlParams = new URLSearchParams() urlParams.set('search', query) window.history.replaceState({}, '', window.location.pathname + '?' + urlParams.toString())"
}